import { EightteenOrOlderDirective } from './eightteen-or-older.directive';

describe('EightteenOrOlderDirective', () => {
  it('should create an instance', () => {
    const directive = new EightteenOrOlderDirective();
    expect(directive).toBeTruthy();
  });
});
